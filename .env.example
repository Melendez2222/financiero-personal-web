# URL base del backend ASP.NET Core (sin barra final). Ej: https://api.misfinanzas.com/api
VITE_API_URL=http://localhost:5080/api

# "true" para usar la capa mock (MSW) y trabajar sin backend real.
# "false" para apuntar a VITE_API_URL.
VITE_USE_MOCKS=true
